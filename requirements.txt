# Dependencies for the Streamlit app (used by Streamlit Community Cloud).
# For the full notebook/pipeline dev environment, see requirements-notebook.txt.
streamlit>=1.60
pandas>=2.0
numpy>=1.26
anthropic>=0.86

# Only needed if you re-run the PDF extraction pipeline (pipeline.py) locally,
# not for running the deployed app itself:
# pdfplumber
# pytesseract
